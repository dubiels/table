DATABASE_PATH=./data/table.sqlite
ALLOWED_EMAILS=you@example.com
RESEND_API_KEY=
EMAIL_FROM="Table <table@yourdomain.com>"
PUBLIC_APP_URL=http://localhost:5173
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
PUBLIC_VAPID_PUBLIC_KEY=
VAPID_SUBJECT=mailto:you@example.com
# Cron expressions run in the container's local timezone (UTC on Fly by default unless TZ is set in fly.toml).
DIGEST_CRON=0 8 * * *
DUE_CHECK_CRON=0 * * * *
DUE_ALERT_LEAD_HOURS=24
DEV_LOG_TOKENS=false
# Canvas LMS assignment feed (the .ics URL from Canvas > Calendar > Calendar Feed).
# Assignments due in the next 14 days are imported.
LMS_ICAL_URL=
LMS_SYNC_CRON=0 */6 * * *
# Deprecated, and safe to leave unset: assignments show in the side panel and list
# view; zone placement is no longer used by the UI. Sync still reads it to pick the
# coordinates a task row needs.
LMS_ZONE_ID=
# Long random string; the Pi wall display sends it as "Authorization: Bearer <token>".
# Unset = /api/dashboard is disabled (404), never open.
DASHBOARD_TOKEN=
# Google Calendar agenda. Create a Desktop OAuth client in Google Cloud, enable
# the Calendar API, publish the consent screen, then run `npm run gcal:auth`
# once to obtain the refresh token. Unset = the panel shows its setup note
# instead of events.
GCAL_CLIENT_ID=
GCAL_CLIENT_SECRET=
GCAL_REFRESH_TOKEN=
# Comma-separated calendar ids (Calendar settings > your calendar > "Integrate
# calendar" > Calendar ID). Unset = the account's primary calendar.
GCAL_CALENDAR_IDS=
# Token for the read-only tasks .ics feed (subscribe in Google Calendar via
# "Other calendars > From URL": https://your-app/calendar.ics?token=...).
# Unset = feed disabled (404).
TASKS_FEED_TOKEN=
