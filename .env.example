DATABASE_PATH=./data/table.sqlite
ALLOWED_EMAILS=you@example.com
RESEND_API_KEY=
EMAIL_FROM="Table <table@yourdomain.com>"
PUBLIC_APP_URL=http://localhost:5173
# On the server this is https://table.mgdubiel.com — magic-link emails are built
# from it, so a wrong value emails people a link to localhost.
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
PUBLIC_VAPID_PUBLIC_KEY=
VAPID_SUBJECT=mailto:you@example.com
# Cron expressions run in the process's local timezone. The Windows service sets
# TZ=America/New_York; set TZ in your shell if you need to match it locally.
DUE_CHECK_CRON=0 * * * *
DUE_ALERT_LEAD_HOURS=24
DEV_LOG_TOKENS=false
# Canvas LMS assignment feed (the .ics URL from Canvas > Calendar > Calendar Feed).
# Assignments due in the next 14 days are imported.
LMS_ICAL_URL=
LMS_SYNC_CRON=0 */6 * * *
# Deprecated, and safe to leave unset: assignments show in the side panel and list
# view; zone placement is no longer used by the UI. Sync still reads it to pick the
# coordinates a task row needs.
LMS_ZONE_ID=
# Long random string; the Pi wall display sends it as "Authorization: Bearer <token>".
# Unset = /api/dashboard is disabled (404), never open.
DASHBOARD_TOKEN=
# Long random string, separate from DASHBOARD_TOKEN so it can be revoked on its
# own. Read/write JSON access to tasks and Dinner Table for an external agent —
# see API.md. Session cookies do not work on these routes. Unset = every
# /api/agent/* route is disabled (404), never open.
AGENT_TOKEN=
# Google Calendar agenda. Create a Desktop OAuth client in Google Cloud, enable
# the Calendar API, publish the consent screen, then run `npm run google:auth`
# once to obtain the refresh token. Unset = the panel shows its setup note
# instead of events.
GCAL_CLIENT_ID=
GCAL_CLIENT_SECRET=
GCAL_REFRESH_TOKEN=
# Comma-separated calendar ids (Calendar settings > your calendar > "Integrate
# calendar" > Calendar ID). Unset = the account's primary calendar.
GCAL_CALENDAR_IDS=
# Google Tasks two-way sync. Needs the Google Tasks API enabled in the same
# Cloud project as the Calendar integration above, and a GCAL_REFRESH_TOKEN
# carrying the tasks scope — re-run `npm run google:auth` after enabling it.
# Unset = the whole feature is off: no cron, no push, no UI controls.
GTASKS_ENABLED=
GTASKS_SYNC_CRON=*/5 * * * *
# Must be unset or false on the server. When true, email.ts prints the login
# link to the log and returns without sending — correct locally, and a silent,
# total auth outage in production.
